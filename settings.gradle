pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url "https://packages.confluent.io/maven/"
        }
        maven {
            url = uri("https://jitpack.io")
        }
    }
}

rootProject.name = 'ManageTask'

include 'api'
include 'core'

